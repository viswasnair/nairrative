name: Deploy to GitHub Pages

on:
  workflow_dispatch: # manual trigger only — site is now hosted on Netlify

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment handled by Netlify"
